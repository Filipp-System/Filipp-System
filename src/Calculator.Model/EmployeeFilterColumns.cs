namespace Calculator.Models
{
    /// <summary>
    /// Sort Options
    /// </summary>
    public enum EmployeeFilterColumns
    {
        LastName,
        PhoneNumber,
        Street,
        City,
        ZipCode,
        Salary,
        Profession,
        UserName
    }
}
