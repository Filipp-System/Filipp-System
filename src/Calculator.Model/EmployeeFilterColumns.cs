namespace Calculator.Model
{
    /// <summary>
    /// Sort Options
    /// </summary>
    public enum EmployeeFilterColumns
    {
        LastName,
        PhoneNumber,
        Street,
        City,
        ZipCode,
        Salary,
        Profession,
        UserName
    }
}
